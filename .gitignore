# dependencies
node_modules
.pnp
.pnp.js

# next.js
.next
out
dist

# typescript
*.tsbuildinfo

# env files — never commit
.env
.env.local
.env.development
.env.development.local
.env.production
.env.production.local

# personal notes / scratchpad — never push to public repo
PROMPT.md

# backend API — its own repository (elfadilisoufiane6/rebelle-store-),
# checked out here for local dev. Committing it would nest a second .git
# and push a broken gitlink instead of the code.
rebelle-backend/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
Thumbs.db

# editor
*.swp
*.swo
